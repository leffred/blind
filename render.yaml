services:
  - type: web
    name: blindtest-server
    env: node
    buildCommand: npm install
    startCommand: cd server && npm run start
    envVars:
      - key: NODE_ENV
        value: production
